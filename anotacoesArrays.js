/*
let carrinho = ['Mouse', 'Monitor','Teclado'];
console.log(carrinho);
console.log(typeof carrinho);

//Como acessar um item em um array

console.log(carrinho[0]);

//Como modificar o item de um array
carrinho[0] = 'Mouse logitec 800dpi';
console.log(carrinho);

//Propriedade de um array

console.log(`Qtd de itens no carrinho = ${carrinho.length}`);
console.log(`Ultimo item do array = ${carrinho[carrinho.length - 1]}`);

*/
//---------------------------------------------------------------------------------
/*
//Looping em array

let carrinho = ['Monitor', 'Mouse', 'Teclado', 'Monitor'];

//Como pecorrer um array

for (let i = 0; i<carrinho.length; i++){ // i = 0 1 2 3
       if(carrinho[i] == 'Monitor'){
        console.log('Tem monitor no carrinho');
       } 
       console.log(carrinho[i]); // carrinho[0] carrinho[1] carrinho[2]
}
*/

//---------------------------------------------------------------------------

//let array = [10,20,30,40,50];
//console.log(array);

//Como adicionar um item ao final do array

//array.push(60);
//console.log(array);

//Como remover o ultimo item  do array
//let excluido = array.pop();
//console.log(array);
//console.log(excluido);

//Como adicionar o primeiro item do array
//array.unshift(15);
//console.log(array);

//Como adicionar em qualquer posição possivel do array
//modos
// 0 -> inserir
// 1 -> apagar
// splice(indice, <modo>, <Valor>)

//array.splice(2,0,333)

//--------------------------------------------------------------------------------

/*
let itens = [ 'Monitor', 'Teclado', 'Mouse', 'SSD'];

//Como saber o indice de um item
//indexOF(<item>);

let indice = itens.indexOf('Monitor');
console.log(`Indice do monitor = ${indice}`)
*/

//----------------------------------------------------------------------------------------------------------------